# Given a reference ID, construct the URL to its profile page

Given a reference ID, construct the URL to its profile page

## Usage

``` r
.get_ref_profile_url(ref_id, is_dev)
```

## Arguments

- ref_id:

  A reference ID

- is_dev:

  Retrieve the dev version of the API base URL?

## Value

The url to the reference profile page
